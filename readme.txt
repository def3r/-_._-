Introducing the `I have ran out of names` OS

Read the Makefile for build, I'm not spoonfeeding.

DEPENDENCIES:
  - Linux kernel image (comiled with `tinyconfig`)
  - editline
  - Look at the Makefile

TIP:
  - Don't forget to use `bear -- make` to generate compile_commands.json
  - Don't forget to RTFMakefile
