Introducing the `I have run out of names` OS

Read the Makefile for build, I'm not spoonfeeding. Please build dependencies on your own (do not light a rainforest on fire)

DEPENDENCIES:
  - Linux kernel image (comiled with `tinyconfig`)
  - editline
  - Look at the Makefile

TIP:
  - Don't forget to use `bear -- make` to generate compile_commands.json
  - Don't forget to RTFMakefile (a rainforest turned to ashes already)
